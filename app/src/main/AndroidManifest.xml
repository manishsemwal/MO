<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manishsemwal.mo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Mathematics Olympiad" />
        <activity
            android:name=".IntroActivity"
            android:label="Introduction"
            android:gravity="center"/>
        <activity
            android:name=".SyllActivity"
            android:label="                          Syllabus" />
        <activity
            android:name=".PreYearActivity"
            android:label="Previous Years"></activity>
        <activity android:name=".BooksActivity"
            android:label="Books"

            >
        </activity>
    </application>

</manifest>